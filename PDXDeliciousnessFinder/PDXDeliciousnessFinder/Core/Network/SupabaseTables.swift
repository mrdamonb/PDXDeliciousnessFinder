import Foundation

/// Central registry of Supabase table names.
/// Use these constants instead of raw string literals.
enum SupabaseTables {
    static let restaurants = "restaurants"
    static let visitLogs = "visit_logs"
    static let profiles = "profiles"
    static let friendships = "friendships"
}

---
topic: Web app S6: feature parity with the iOS app
updated: 2026-09-08T18:17
---

- (note) Source: spec-wip.md (Web App S6, drafted 2026-09-01, status draft, never approved). This run refreshes it against the repo as of 2026-09-06.
- (direction) Damon 2026-09-06: scope is feature parity only. 'Mobile site' means the Vercel web app, which already works on his phone; no responsive or layout work in scope.
- (event) Parity audit re-verified against web/src on 2026-09-06 by reading code, not carried over from the 2026-09-01 audit.
- (decision) Menu at the point of logging LEAVES scope: web already ships it. RestaurantPanel.tsx renders a View menu link inside the inline log form (L444), EditRestaurantModal has a menu_url field (L260), restaurants.ts carries menu_url. The 2026-09-01 audit row marking this a gap is stale.
- (decision) Web's menu link deliberately does NOT fall back to website while iOS does (AddVisitView.swift:73). Recorded on the web side as code review 2026-09-02 decision a: the panel's existing Website row would render a duplicate link. Preserved as a deliberate divergence, not closed.
- (capability) CAP-4 (edit a visit on web) enters scope: iOS story 2.10 postdates the S6 draft entirely. Web actions.ts has getVisitLogs and logVisit and no update path.
- (constraint) Web edit must write visit_logs.updated_at. iOS 2.10 resolves conflicts on dto.updatedAt > existing.updatedAt; web's visit select (actions.ts:97) omits updated_at today, so a web edit that omits it cannot participate in last-write-wins.
- (question) Story 2.10 status contradiction, unresolved: spec-2-10-edit-a-visit.md frontmatter says status done, sprint-status.yaml says review. Flagged, not averaged.
- (note) check
- (note) Disregard the preceding 'check' entry: an accidental write-verification append, no content. Log is append-only so it stays on the record rather than being edited out.
- (capability) CAP-1 search your own list on web, mirroring iOS 3.5 (done, device-verified 2026-09-02). Web filters.ts FilterState has no query field; no search input on any web surface.
- (capability) CAP-2 visit journal across all restaurants on web, mirroring iOS 2.7 (done). No History component exists in web/src; actions.ts has getVisitLogs (per restaurant) and no getAllVisitLogs.
- (capability) CAP-3 add a visit from the journal on web, mirroring iOS 2.8 (done, device-verified 2026-09-05). Depends on CAP-1 for the restaurant picker and CAP-2 for the surface.
- (constraint) Do not change web's logVisit auto-promotion of want_to_go to been_there. It is the correct behaviour and iOS 2.8 adopted it; web is already right here (actions.ts:227).
- (question) Divergence 3, unresolved: 'Latest' sorts by updatedAt on iOS and created_at on web, same label, different question. The 2026-09-01 draft proposed created_at on both. That was a draft proposal, never approved by Damon, so it stays open.
- (question) History navigation shape, unresolved since 2026-09-01: separate route (/history) or a third toggle beside map and list. Marked Damon's call in the draft; touches the header, the one area with a documented repaint-regression history (c096d6a, c036895).
- (question) Unverified since 2026-09-01: how many restaurants actually have website populated. Registered as an open loop in the vault. Lower stakes for S6 now that menu is out of scope, but still gates how much the menu link earns on either surface.
- (decision) Success signal ties to a real blocked check: story 2.10's cross-device conflict verification was deferred by Damon specifically until web reaches parity, to use web as 'device B' instead of a second iOS device. S6 landing is what unblocks it.
- (event) Self-validate pass 1 (coherence): PASS. All four capabilities carry intent+success; intents are WHAT not HOW; every constraint rules something out; eight non-goals; success signal is testable (story 2.10's conflict check); CAP-1..4 unique and stable in a fresh workspace.
- (event) Self-validate pass 2 (preservation): PASS. Every load-bearing claim from spec-wip.md landed in SPEC.md or a companion. Wrapper-only content dropped: the frozen-after-approval tag and the 'Open before approval' heading. Menu-related I/O rows and key files dropped deliberately, not silently, because that capability shipped on web (logged above as a decision).
- (note) Artifacts written: SPEC.md (4 capabilities), parity-audit.md, edge-cases.md, build-plan.md. Adopted companion: ../../project-context.md. Source: ../../implementation-artifacts/spec-wip.md.
- (note) Sally (bmad-agent-ux-designer) RECOMMENDS the third segment in the existing Map/List toggle over a separate route, on three grounds: the UX spec already treats these as peer destinations (line 227, tab bar up to 4), it preserves the 'no re-filtering when switching' rule (line 83), and it matches the iOS third tab so Damon's muscle memory transfers across his own two surfaces. Named price: the header title already truncates on a phone (whiteSpace nowrap + ellipsis, minWidth 0) and a third icon button costs it roughly another 37px. STILL DAMON'S DECISION, not settled.
- (decision) DAMON DECIDED 2026-09-06: the journal is a third segment in the existing header Map/List toggle, not a separate route. Closes the open question carried since 2026-09-01.
- (direction) DAMON, 2026-09-06, correcting Sally's framing: the journal is not only reflective, it feeds the decision of where to go tonight, because it answers 'where have I been recently and what did I have?' This makes it a genuine peer of Map and List in the decision loop, not a side room.
- (constraint) CAP-2 consequence of the above: recency and what-was-eaten must be legible at a glance in the journal, since the journal is a decision surface and not only an archive. The note field is where 'what I had' lives today.
- (decision) DAMON DECIDED 2026-09-06: replace the header wordmark with the existing app logo to buy back the width the third segment costs.
- (event) Logo asset inspected 2026-09-06 (Assets.xcassets/Logo.imageset/Logo.png): 1024x1024, pin-with-PDX-and-cutlery. Three blockers to dropping it in as-is: background is near-white (254,254,253) not the app's warm #F7F3EE so it would read as a white square on the blurred header; pin fill is #E14729, not the app's #C2410C; artwork occupies only 48% of canvas width, so at 28px the pin would render about 13px. Also web/ has no public/ directory at all.
- (note) Sally RECOMMENDS redrawing the pin as an inline SVG rather than shipping the PNG: the header's existing Map and List icons are already inline SVG components in HomeView.tsx, so it matches the established pattern, stays crisp, and adds no image request. Colour choice (match #C2410C or keep the logo's #E14729) is STILL DAMON'S, not settled.
- (decision) DAMON DECIDED 2026-09-06: the header logo pin renders in #C2410C, the app's orange, not the logo file's #E14729. One orange in the app.
- (decision) Story breakdown agreed with Damon 2026-09-06, five stories in execution order: 1 search, 2 logo replaces wordmark, 3 journal plus third segment, 4 add a visit from the journal, 5 edit a visit.
- (decision) Story 2 is deliberately split out from story 3. The header carries two prior repaint regressions (c096d6a, c036895); landing the logo swap and the third segment together would mean bisecting two changes at once. Alone it is independently shippable and buys the width before anything needs it.
- (decision) Checkpoints confirmed by Damon: spec_checkpoint on stories 2 and 5; done_checkpoint on story 5, which is where the deferred iOS 2.10 cross-device conflict check gets run. All others run through.
- (event) stories.yaml written and checked against assets/stories-schema.md: 5 entries, ids 1-5 quoted strings, unique, prefix-free, no status field, all required fields present. VALID.
- (decision) DAMON DECIDED 2026-09-06: story 2.10 is done. Resolves the status contradiction; sprint-status.yaml updated from review to done. CAP-4 therefore builds against settled iOS behaviour, and 2.10's cross-device conflict check moves to story 5 of this spec rather than remaining a gate on 2.10.
- (decision) DAMON DECIDED 2026-09-06: retire spec-wip.md. Replaced in place with a superseded stub pointing here and naming the two claims that had gone stale; full original text preserved in git at 370c9af.
- (note) Observed while updating sprint-status: every story in Epic 2 is now done (2-1 through 2-10), which by the file's own definition would make epic-2 'done'. Left at in-progress; closing the epic is Damon's call, not derived automatically.
- (direction) DAMON 2026-09-06: 90% confident story 1.5 works; does not want the web parity work gated on device-verifying it, because the web app is itself the verification path. Loop resolved as not-a-blocker rather than deferred.
- (direction) DAMON 2026-09-08: builds the web parity stories in a separate window, same pattern as iOS 3.5, 2.9 and 2.10. This session plans and reviews; it does not implement. Answers the question left open on 09-06.
